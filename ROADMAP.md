# 学习路线：从前端到 Agent 应用工程

依据 MISSION.md：每周 4–6 小时，首轮约 12 周（48–72 小时）。周次是安排参考；能解释、能实现、能排查、能迁移才是推进依据。

## 贯穿项目

暂选“研发知识助手”，以虚构的前端组件文档和工单为输入：回答使用问题、检索依据、查询工单、提出处理建议，并在明确批准后创建演示工单。由 CLI 起步，再接入 Web UI；不直接连接公司系统。

每阶段保留一个可运行版本与少量失败案例，最后形成可展示的作品与设计说明。最初不一次性生成整个应用，核心功能由学习者在练习中完成。

## 能力阶段与验收

| 参考周次 | 学什么 | 做什么 | 如何证明学会 |
| --- | --- | --- | --- |
| 1 | 模型/程序/工具职责；消息、上下文、token；首次服务端调用 | CLI 单次问答，记录输入输出、耗时和失败 | 解释数据流，独立改请求，处理一次失败；建立 5 条小样本 |
| 2 | 提示、结构化输出、运行时校验 | 将工单描述提取为结构化字段 | 能区分“符合结构”和“内容正确”，处理无效输出；补齐边界样本 |
| 3–4 | 工具定义与参数校验；执行循环；停止条件、超时和只读权限 | 手写一个有限步数的 TypeScript 工具循环 | 从轨迹说清谁做决定、谁执行；修复未知工具、工具失败和重复请求 |
| 5 | Web 流式交互、取消、状态与人工确认 | 页面显示任务进度、工具结果和错误；服务端保管密钥 | UI 能区分等待/调用/完成/失败，取消任务可观察；恢复交互不重复写入 |
| 6–7 | 检索、RAG、引用；上下文预算与记忆边界 | 接入小型文档集，比较关键词/向量检索，选择相关证据 | 区分找不到材料和生成错误；证据不足时说明限制；评估引用与用户隔离 |
| 8 | 持久状态、检查点、幂等与恢复 | 中断后恢复演示任务；加入需批准的写工具 | 重启、重试和重复提交不会重复创建演示工单，权限由程序检查 |
| 9 | 评估、trace、成本/延迟；提示注入与越权 | 系统化失败样本和回归测试，观察关键路径 | 用数据比较改动；构造恶意文档样本，验证程序边界而非依赖提示自觉 |
| 10 | 框架与协议：AI SDK、LangGraph、MCP | 选一个合适抽象迁移部分手写逻辑，连接一个本地只读 MCP 工具 | 说明框架替你管理了什么；能区分协议、执行循环、工具与数据存储 |
| 11–12 | 综合项目、运行维护、架构表达与能力迁移 | 完成可复现运行/部署、评估报告、演示和新需求 | 离开逐行提示独立做一项变更，解释取舍，并根据轨迹定位一次故障 |

这是教师根据当前起点作出的课程设计。技术依据见 RESOURCES.md；进度会根据练习调整。安全与评估从首次调用、首次工具就开始，后期再系统深化。多 Agent 协作、浏览器操作和更复杂的规划属于基础阶段后的选修，需要用评估证明收益后再引入。

## 每周怎么学

- 两次 45 分钟：每次完成一到两节 10–15 分钟短课、回忆旧题和小练习。
- 一次 90–150 分钟：自己实现本周项目增量，教师按实际卡点给提示与评审。
- 一次 60 分钟：查原始资料、排错与复盘，隔次用新题检验同一概念。

合计 4–5 小时，剩余时间用于困难点。忙的时候减少新内容，保留一次提取练习和一次动手。

## 最终作品的初始验收建议

- 从 README 能在新环境启动项目，环境变量示例不包含密钥。
- 至少 30 个有明确成功条件的评估任务，覆盖正常、缺信息、工具失败、权限和恶意输入；这是学习用的起始规模，不代表生产覆盖充分。
- 对关键任务重复至少 3 次，报告成功/失败、延迟与成本，不隐藏失败样本；具体阈值结合任务难度确定。
- 展示正常任务、证据不足、工具异常和人工确认四段演示。
- 解释架构、已知限制，以及为何某处采用固定流程或模型决策。

## 现在只做一件事

完成 lessons/0001-who-controls-the-next-step.html，将末尾迁移题的答案与理由发回对话。不要求提前购买 API 或安装框架。
