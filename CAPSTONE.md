# 综合项目任务书：研发知识助手

目标：做出一个你能独立解释、修改和排查的 TypeScript Agent 应用，用来展示从前端转向 Agent 工程的能力。教材已完整备齐；项目代码由你在学习中逐步完成。

## 用户与任务

给一个维护前端组件库的团队做助手。它能检索虚构组件文档、读取虚构工单、按证据解释行为或提出排查建议，并在用户确认具体内容后创建演示工单。至少两个演示租户，不接入真实公司系统。

初始语料与 30 条任务种子在 `labs/data/`。关键词检索即可建立第一个基线，之后用数据决定是否采用 embedding、reranker、agentic search 或多个 Agent。

## 分层与数据流

1. Web UI：展示回答、来源、工具阶段、失败与批准界面；可取消和恢复查看。
2. Run API：认证、租户/用户绑定、预算与 runId；密钥留在服务端。
3. Harness：模型适配、执行循环、上下文选择、可观察事件与停止原因。
4. 工具：searchDocs、readIssue、proposeIssue、executeApprovedIssue；权限检查独立于模型文本。
5. 状态与观测：会话/操作/批准/收据/事件存储，评估结果及脱敏日志。

这些职责可以先处于同一个进程，不要求为了分层部署多个服务。SDK、数据库和托管方式由你选择并写下理由。

## 四个可验收增量

### A · 只读 Agent（建议 3–4 小时）
- 完成真实模型适配、searchDocs、readIssue 和有限工具循环。
- 用量/耗时由实际 API 返回和计时器记录，输出来源能定位到文档版本。
- 演示成功、缺信息、无权限、工具超时四种情况；各存一条脱敏 trace。

### B · 产品交互与写操作（建议 3–4 小时）
- UI 区分模型输出、工具执行、等待批准和终态；取消结果不会被旧响应覆盖。
- proposeIssue 只生成待确认对象；批准绑定服务器保存的参数、用户、租户与操作版本。
- 执行写入检查批准并带幂等键；重复点击、网络重试不重复创建演示工单。

### C · 长任务与能力扩展（建议 3–5 小时）
- 一个任务可在进程重启后恢复；说明崩溃位于工具提交前/后时的恢复路径。
- 接入一个本地只读 MCP 工具，或实现一个按需加载的 Skill；这是二选一核心要求，另一个可选做。
- 单独试验一个扩展：动态工具发现、子 Agent、隔离代码执行或浏览器操作。保留关闭扩展的基线，用任务结果/成本决定是否保留；不要为凑概念全部引入。

### D · 评估与交付（建议 3–5 小时）
- 将 30 条种子补成可运行评估：明确 fixture、身份、预期结果、禁止行为、评分规则。
- 关键场景重复至少 3 次，记录每次结果、失败原因、延迟与成本；报告模型/提示/工具/数据版本。
- 写 README、架构图、设计取舍、已知限制、可复现启动方式和演示脚本。
- 找一个未参与开发的人试用或请教师进行盲测，记录与自己的预期不同的发现。

## 验收与评分（总分 100）

| 维度 | 分值 | 证据 |
| --- | ---: | --- |
| 任务完成与证据质量 | 25 | 正确查证、无资料时明确限制、引用可追溯 |
| 执行可靠性 | 20 | 循环界限、异常、取消、恢复和幂等实验 |
| 权限与信任边界 | 20 | 跨租户、注入、批准绕过和工具越权样本 |
| 可观测与评估 | 20 | trace、评分器、重复试验与失败分析 |
| 产品体验与工程表达 | 15 | UI、可复现性、设计说明与独立变更 |

未经批准的副作用、跨租户数据泄露、密钥进入浏览器/日志是必须修正的缺陷，不能靠其他得分抵消。初始课程目标建议总分至少 80 且上述边界样本全部通过；这只是教学标准，不能证明生产系统不存在其他漏洞。

## 交付目录建议

```
project/
  app/              # 前端、API与Agent运行时代码
  evals/            # fixtures、cases、graders、逐次结果
  docs/             # 架构、取舍、故障复盘
  .env.example      # 只含变量名和说明
  README.md
```

## 独立迁移任务

在不看参考实现的情况下，加入“跨版本 API 差异解释”：用户给组件名与两个版本，助手找出相应证据并回答差异。若只找到一个版本，必须说明缺失。先写 5 个验收任务，再实现；不要把原有租户过滤和成本统计丢掉。

教师的评审重点：你如何判断失败发生在检索、决策、执行或生成；你为什么保留/删除某个框架或子 Agent；更换模型时哪些假设需要重新评估。参考思路见第 0040 课，但先自己完成。
